@model UnamiSushi.Models.MenuCategory

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>MenuCategory</h4>
    <hr />

            @Html.DisplayFor(model => model.CategoryName)

            @Html.DisplayFor(model => model.CategoryDescription)


    <hr />
    <dl class="dl-vertical">
        @foreach (var item in Model.MenuItems)
        {
            <div>    
                @*string imageString = item.ItemGallery.FirstOrDefault().PicturePathname; 
                <img src="@imageString" alt="image" width="200" height="150" />*@
                
                @Html.DisplayFor(item => item.MenuItemName)
                
                @Html.DisplayFor(item => item.MenuItemDescription)
                
                @Html.DisplayFor(item => item.MenuItemPrice)
            </div>
        }
    </dl>
</div>
<p>.
    @Html.ActionLink("Edit", "Edit", new { id = Model.CategoryID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
